# requirements.txt
# GPT-PRODUCTION-LEVEL — Dependencies
#
# Install: pip install -r requirements.txt

torch>=2.2.0
tiktoken>=0.5.1
matplotlib>=3.7.0
pyyaml>=6.0
tqdm>=4.65.0
requests>=2.28.0
pytest>=7.0.0
