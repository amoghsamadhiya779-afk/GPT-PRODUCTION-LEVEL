# requirements.txt
# GPT-PRODUCTION-LEVEL — Dependencies
#
# Install: pip install -r requirements.txt
#
# Pinned to exact versions (verified against the full test suite) so a fresh
# Docker/Space build can't silently pull a breaking major. Bump deliberately:
# update a version, re-run `pytest tests/`, then commit the change.

torch==2.10.0
tiktoken==0.12.0
pyyaml==6.0.3
requests==2.32.5
fastapi==0.138.2
uvicorn==0.49.0
pydantic==2.13.4
streamlit==1.54.0
beautifulsoup4==4.14.3
slowapi==0.1.10
transformers==5.11.0
